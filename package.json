{
  "private": true,
  "scripts": {
    "start": "parcel serve index.html",
    "build": "parcel build --no-cache --no-source-maps --no-autoinstall --no-content-hash --experimental-scope-hoisting --public-url /barbaros index.html",
    "lint": "tsc -w --noEmit",
    "gh-pages": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/ramda": "^0.26.9",
    "@types/react": "^16.8.19",
    "@types/react-dom": "^16.8.4",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-bundle-visualiser": "^1.2.0",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "@blueprintjs/core": "^3.15.1",
    "ramda": "^0.26.1",
    "react": "^16.8.0",
    "react-dom": "^16.8.0"
  }
}
